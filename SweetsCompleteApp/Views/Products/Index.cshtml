@using PagedList.Mvc;
@using PagedList;
@model IPagedList<SweetsCompleteApp.product>
    @{
        ViewBag.Title = "Products";

        /*var itemsPerPage = 6;
        var x = 0;
        var j = 1;



        string url = HttpContext.Current.Request.Url.AbsoluteUri;
        string fixedurl = Request.RawUrl.Split(new[] { '?' })[0];
        string newParam = Request.Params["page"] ?? "0";
        int getPageNum = Convert.ToInt32(newParam);
        if (getPageNum == 0)
        {
            itemsPerPage = 6;
        }
        else if (getPageNum >= 1)
        {
            itemsPerPage = 6 * (getPageNum + 1);
        }

        if (IsPost)
        {

        }*/
    }

    <style>
        .container {
            text-align: center;
        }
    </style>

    <h2 class="text-center">Our Products</h2> 
    <input type="text" class="span2 search-query capitalizedText" id="que" name="query">
    <button type="submit" class="btn">Search</button>
                     <div class="container" style="width: 100%;">
                         <ul style="list-style-type: none; width: 70%; margin-left: auto; margin-right: auto; float: none; ">
                             <div class="container" style="margin-left: auto; margin-right: auto; float: none; ">
                                 @foreach (var item in Model)
                                 {
                                     <li style="display: inline-block;">
                                         <div class="container">
                                             <img src="~/Content/Images_fixed/@(item.link).scale_20.jpg" width=" 190" height="130" />
                                             <p>
                                                 @Html.DisplayFor(modelItem => item.title)
                                                 <br />
                                                 @Html.DisplayFor(modelItem => item.price)
                                                 <br />
                                                 <button class="btn btn-default"> Add to Cart </button>
                                             </p>
                                        </div>
                                    </li>
                                 }
                             </div>
                         </ul>
                     </div>

                     <form role="form">
                         <div class="radio">
                             <h4>By Price</h4>
                             <label><input type="radio" name="optradio">Low</label>
                             <br />
                             <label><input type="radio" name="optradio">High</label>
                         </div>
                         <div class="radio">
                             <h4>By Popularity</h4>
                             <label><input type="radio" name="optradio">Least</label>
                             <br />
                             <label><input type="radio" name="optradio">Most</label>
                         </div>
                         <div class="radio">
                             <h4>By Specials</h4>
                             <label><input type="radio" name="optradio">Specials</label>
                         </div>
                     </form>

    @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))



